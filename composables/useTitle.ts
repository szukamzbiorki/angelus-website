export const useTitle = () => {
    return useState('title', () => '')
}

export const useMedium= () => {
    return useState('medium', () => '')
}

export const useSize = () => {
    return useState('size', () => '')
}

export const useYear = () => {
    return useState('year', () => '')
}